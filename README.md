# shimpz-core

Audited privilege-broker sidecars — each holds exactly one credential and exposes a narrow, validated, audited HTTP API, so the agent never touches a secret directly.

---
Part of the **[Shimpz](https://github.com/TheShimpz/shimpz)** stack — a self-hosted, voice-driven autonomous agent.
